const asset = require('assert');
const { Given, When, Then } = require('@cucumber/cucumber');

Given('I have \\{{int}} clientes in my database', function (qtd) {
  return 'pending';
});

When('I access the home webpage', function () {
  return 'pending';
});

Then('I see the list of \\{{int}} itens', function (qtd) {
  return 'pending';
});
